{
  "name": "load-test",
  "version": "0.0.1",
  "description": "Script for simulating traffic from a \"recorded\" csv ",
  "main": "index.js",
  "bin": {
    "load": "./cli.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "standard"
  },
  "dependencies": {
    "undici": "^7.16.0"
  },
  "devDependencies": {
    "fastify": "^5.6.0",
    "standard": "^17.1.2"
  }
}
